// Declare variables using var, let, and const
var name = "Alice"; // Function-scoped
let age = 25;       // Block-scoped
const isTester = true; // Block-scoped, cannot be reassigned

//print variable values
console.log(name, age, isTester);
